python -m venv venv - Create Python virtual environment
docker compose exec airflow-scheduler airflow db init - Initialize database (first-time only)
docker compose run airflow-webserver airflow db migrate - Apply schema updates (after upgrades)
pip install -r requirements.txt
docker compose exec airflow-webserver airflow users create --username admin --firstname Imani --lastname Mike --lastname k --role Admin --email michaelimani396@gmail.com --password password123 #User "admin" created with role "Admin" 
docker rm -f kafdrop

